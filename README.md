# clinic app is a web app built with the robust power of node.js, express.js, javascript and mongodb.
# It runs on node.js runtime environment.
# cd into project folder and run node app to start the server.
